openai>=1.60
httpx>=0.27

# Optional, only for the Search Console auto-pull:
# google-auth>=2.30
fastapi
uvicorn

# Browser-automation posting (run `playwright install chromium` after pip install):
playwright
